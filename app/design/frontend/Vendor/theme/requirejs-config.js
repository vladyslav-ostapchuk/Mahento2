var config = {

    deps: [
        "js/main",
    ],

    map: {
        '*': {
            'select': 'js/jquery.nice-select',
        }
    },
    "shim": {
        "select": ["jquery"],
    },
};

